---
title: "重新搭建我的个人博客"
date: "2026-06-07"
tags: ["博客", "Next.js", "写作"]
excerpt: "记录一次从空白站点到个人数字花园的搭建过程，以及内容系统的组织方式。"
featured: true
draft: false
---

# 重新搭建我的个人博客

这是这个个人博客的第一篇文章，用来记录站点搭建时的思路。

我希望它不仅是一个展示页面，也是一套可以长期维护的写作系统：文章放在 `content/essays` 目录中，项目放在 `content/projects/projects.json` 中，个人介绍放在 `content/about.mdx` 中。

## 为什么要这样组织？

本地内容文件有几个优点：

- 写作体验简单直接；
- 适合版本管理；
- 构建时生成静态页面，访问速度快；
- 后续迁移到 CMS 或数据库时，也有清晰的数据边界。

## 示例代码块

```ts
export function sayHello(name: string) {
  return `你好，${name}`;
}
```

后续可以继续扩展标签归档、RSS、搜索和更多项目复盘，让这个站点逐渐变成长期维护的内容系统。
